`pask-site` is the reference site-owner producer for the `wilder.pser/0.5` profile (the `pask-wire` `SPEC_VERSION`; the `-04` draft also defines `wilder.pser/0.6`): it canonicalizes deterministic engagement evidence, constructs and signs a validated receipt, and hands the signed bytes to a verify-before-push `pask-adapter` implementation.
