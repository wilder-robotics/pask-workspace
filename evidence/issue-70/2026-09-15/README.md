# Issue 70 local verification evidence

This directory records local execution against source tree
`1bb6d99f2d3e13ee62f8067c99d4f5ab55b76e17`, based on main commit
`51dde6de80af2fc6aa661f88dbe0bc227848888e`. Adding this evidence directory
changes the published tree, not the tested Rust source. The PR head and hosted
run identify the complete published tree.

The previously reviewed repair tree was
`13cd44c61416059bcb4198bf5a191e27e8b4d51d`.
`reviewed-to-current.patch.gz` contains only the ten format-string changes,
the separate limitations clarification, and the final stable lint fix.
`pask70-publish-source-manifest.json` hashes all 163 tested source paths.

## Actual commands and results

Resolved Rust and Cargo: 1.98.1. Clippy: 0.1.98.
The individual transcripts and results JSON record full versions,
environment, exact commands, exit codes, timings, and test counts.

```text
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo build --workspace --all-features
cargo test --workspace --locked
cargo test --workspace --features es256 --locked
```

All five commands passed. Default tests: 274 passed, 0 failed, 2 ignored.
ES256 tests: 280 passed, 0 failed, 2 ignored. These are separate feature
runs, not a combined count of distinct tests. The transcripts contain
each executed test name.

The ignored tests remain `mode_defense_in_depth` (the wire validator rejects
every non-WRITE_ONLY mode) and `real_ledger_submit_and_retrieve_receipt`
(requires a real ledger). Neither is counted as a pass or interoperability.

Local runs used a Linux source-specific build directory, incremental
compilation disabled, and development/test debug information disabled.
They are not a claim of hosted CI success or operating-system parity.

## Scope

The actual sender and reader are exercised, including both profile versions,
transmitted tag 18, signed-content and encoded-Receipt preservation,
independent vectors, noncanonical signed contents, malformed containers,
and valid/invalid additional attachments in both orders and all-invalid cases.
The independent fixture files were not regenerated by this cleanup.

These checks do not establish full inner-Receipt validation, authenticated
service or witness key association, hardware provenance, application-facing
aggregate verification, or independent-service interoperability.

No DCO certification is represented by these logs. No merge, release,
profile change, or IETF submission is implied.
